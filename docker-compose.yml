version: '3.8'

services:
  db:
    image: postgis/postgis:latest
    container_name: citytwin-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: citytwin
    ports:
      - "5432:5432"
